# URL of the FastAPI chat proxy. Local dev: http://127.0.0.1:8000. Production
# build (Hostinger): https://api.brianchenhao.com. Vite inlines this at build
# time — there is no runtime config, you must rebuild after changing it.
VITE_API_URL=http://127.0.0.1:8000
